name: Hubspot IA Agent Full

on:
  workflow_dispatch:
  schedule:
    - cron: '45 8 * * *'
    - cron: '0 18 * * *'

jobs:
  run-all:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Clonar repositório
      uses: actions/checkout@v3

    - name: 🐍 Instalar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: 📦 Instalar dependências
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: 🧠 Rodar processador de prompts
      run: python scripts/processar_prompts.py
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        GSHEETS_KEY_B64: ${{ secrets.GSHEETS_KEY_B64 }}

    - name: 🧪 Rodar diagnóstico da planilha
      run: python scripts/diagnostico_planilha.py
      env:
        GSHEETS_KEY_B64: ${{ secrets.GSHEETS_KEY_B64 }}
